import defineStructure from "./defineStructure";

export const createHttpData = defineStructure([
    "body"
]);

export const createProduct = defineStructure([
    "title",
    "href",
    "unitPrice"
]);